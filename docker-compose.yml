version: "3"
services:
  node_connection:
    container_name: node_connection
    image: med2bouanane/node_connection
    build:
      context: .
    restart: always
    command: ./wait-for.sh mongo_connection:2717 -- npm start
    depends_on:
      - mongo_connection
    ports:
      - "3000:3000"
    networks: 
      - net_node
    env_file: .env
  mongo_connection:
    container_name: mongo_connection
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    networks: 
      - net_node 
    volumes:
     - ./data/mongodb:/data/db

networks: 
  net_node: